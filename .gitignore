# Dependencies: local packages installed via npm
node_modules/

# Production: build output (generated by Vite or other bundlers)
dist/

# Security: environment variables and secret keys
.env

# System Files: macOS specific files (prevents clutter when switching to Windows)
.DS_Store

# Testing: code coverage reports
coverage/

# Editor: local VS Code settings and preferences
.vscode/